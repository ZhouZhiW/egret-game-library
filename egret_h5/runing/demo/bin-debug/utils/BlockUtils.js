var __reflect = (this && this.__reflect) || function (p, c, t) {
    p.__class__ = c, t ? t.push(c) : t = [c], p.__types__ = p.__types__ ? t.concat(p.__types__) : t;
};
var BlockUtils = (function () {
    function BlockUtils() {
    }
    /**
     * 创建障碍物等物品
     */
    BlockUtils.createBlock = function (runx, layerOnjects) {
        // let 
        for (var obj in layerOnjects) {
            // layerOnjects
        }
    };
    BlockUtils.removeBlock = function () {
    };
    return BlockUtils;
}());
__reflect(BlockUtils.prototype, "BlockUtils");
//# sourceMappingURL=BlockUtils.js.map
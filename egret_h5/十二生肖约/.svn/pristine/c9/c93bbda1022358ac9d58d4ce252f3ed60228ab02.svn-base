class TestBG extends eui.Image {
    public constructor() {
        super();
        this.alpha = 1;
        this.source = "resource/res/base/base_black_bg.png"
        // this.once(egret.Event.ADDED_TO_STAGE, this.init, this);
    }
    private init() {
        if (this.width < 70) {
            this.width = this.parent.width;
            this.height = this.parent.height;
        }

    }
}